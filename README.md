# BallTrace

基于 `uni-app + Vue 3 + Vite + JavaScript + Pinia` 的项目初始化模板，已兼容移动端与 H5 开发。

## 启动

```bash
npm.cmd install
npm.cmd run dev:h5
```

## 已包含

- `uni-app` 标准目录结构
- `Vue 3` 语法
- `Pinia` 状态管理
- `Vite` 构建配置
- H5 / 移动端首页适配样板
